# 📋 Web自动化测试 - 快速参考卡片

> 打印或保存在桌面，随时查阅

---

## 🚀 常用命令

```bash
# 运行测试
python main.py --type=yaml --cases=./examples/web-cases-yaml

# Excel 用例
python main.py --type=excel --cases=./examples/web-cases-excel

# 自定义关键字目录
python main.py --type=yaml --cases=./my-cases --keyDir=./my-keywords
```

---

## 📁 项目结构速记

```
main.py                    # 程序入口
HAT/
├── core/
│   ├── CasesPlugin.py     # pytest 插件
│   ├── TestRunner.py      # 测试执行器
│   └── globalContext.py   # 全局上下文
├── keywords/
│   └── web_keywords.py    # Web 关键字库
├── parse/
│   ├── YamlCaseParser.py  # YAML 解析器
│   └── ExcelCaseParser.py # Excel 解析器
├── context/
│   └── WebCaseContext.py  # Web 上下文
└── utils/
    ├── VarRender.py       # 变量渲染
    └── allure_step_logger.py  # 日志收集
examples/                  # 示例用例
```

---

## 📝 YAML 用例模板

```yaml
基础配置:
  用例类型: WebCase
  一级模块: 模块名
  二级模块: 功能名
  用例标题: 用例标题

用例步骤:
- 步骤名:
    操作类型: 关键字
    _页面元素: 元素名
    数据内容: 值

数据驱动:
  - 变量1: 值1
    变量2: 值2
    描述标题: 描述
```

---

## 🔑 常用关键字

| 关键字 | 必需参数 | 示例 |
|--------|---------|------|
| 访问网址 | 网址 | `网址: http://example.com` |
| 输入内容 | _页面元素, 数据内容 | `_页面元素: 用户名, 数据内容: admin` |
| 点击元素 | _页面元素 | `_页面元素: 登录按钮` |
| 强制等待 | 数据内容 | `数据内容: '3'` |
| 窗口最大化 | - | - |
| 获取元素文本 | _页面元素, 变量名 | `变量名: my_var` |
| 断言文本包含 | 预期结果, 实际结果 | `预期结果: 欢迎, 实际结果: '{{text}}'` |
| 断言文本相等 | 预期结果, 实际结果 | `比较符: ==` |
| 刷新页面 | - | - |

---

## 🎯 元素定位方式

| 定位方式 | YAML 写法 | Selenium 对应 |
|---------|----------|--------------|
| ID | `定位方式: id` | `By.ID` |
| 名称 | `定位方式: name` | `By.NAME` |
| 类名 | `定位方式: class` | `By.CLASS_NAME` |
| XPath | `定位方式: xpath` | `By.XPATH` |
| CSS | `定位方式: css selector` | `By.CSS_SELECTOR` |

---

## 📦 context.yaml 配置模板

```yaml
session_reuse: False  # 是否复用浏览器

_浏览器:
  capability:
    browserName: chrome  # chrome / firefox / ie

_WEB页面元素:
  元素名:
    定位方式: id
    目标对象: element_id
```

---

## 🔍 调试技巧

### 1. 查看详细日志
```bash
# 修改 main.py 中的控制台日志级别
{"sink": sys.stdout, "level": "DEBUG"}
```

### 2. 打印变量
在用例步骤中添加：
```yaml
- 打印调试信息:
    操作类型: 强制等待
    数据内容: '{{my_variable}}'
```

### 3. 使用前置脚本
```yaml
前置脚本:
  - "print('当前变量:', context)"
  - "context.update({'debug_var': 'test_value'})"
```

### 4. 暂停观察
```yaml
- 暂停查看页面:
    操作类型: 强制等待
    数据内容: '10'  # 暂停10秒
```

---

## ⚠️ 常见问题速查

| 问题 | 解决方案 |
|------|---------|
| 找不到元素 | 检查元素定位是否正确，页面是否加载完成 |
| 变量未替换 | 检查 context.yaml 中是否定义了该变量 |
| 浏览器未关闭 | 检查 `session_reuse` 配置 |
| 用例未执行 | 检查文件名是否以数字开头（如 `1_xxx.yaml`） |
| Allure 报告未生成 | 确认 allure 工具已安装并配置环境变量 |
| 依赖安装失败 | 检查 Python 版本 >= 3.8 |

---

## 💡 代码片段

### 添加自定义关键字
```python
# 在 HAT/keywords/web_keywords.py 中添加

@allure.step("自定义操作")
def 自定义操作(self, **kwargs):
    """自定义操作说明"""
    # 你的代码
    self.get_screenshot()
```

### 添加前置脚本
```yaml
前置脚本:
  - "context.update({'username': '13800138001'})"
  - "import datetime; context.update({'time': str(datetime.datetime.now())})"
```

### 使用断言
```yaml
- 验证结果:
    操作类型: 断言文本包含
    预期结果: 欢迎
    实际结果: '{{page_text}}'
    错误信息: 登录失败，未看到欢迎文本
```

---

## 📊 执行流程图解

```
main.py (入口)
  ↓
CasesPlugin (解析参数)
  ↓
YamlCaseParser (读取用例)
  ↓
g_context (加载配置)
  ↓
TestRunner (执行测试)
  ↓
WebCaseContext (初始化浏览器)
  ↓
Keywords (执行关键字)
  ↓
Allure (生成报告)
```

---

## 🎓 学习路径

```
Day 1: 环境搭建 ✓
Day 2: YAML 用例 ✓
Day 3: 关键字库 ✓
Day 4: 数据驱动 ✓
Day 5: pytest 插件 ✓
Day 6: 全局上下文 ✓
Day 7: 实战项目 ✓
```

---

## 📚 扩展阅读

- 《学习指南_从零到精通.md》- 完整学习指南
- 《7天快速上手指南.md》- 7天学习计划
- 《项目检查报告.md》- 项目问题与优化建议

---

## 🔗 有用链接

- pytest 文档: https://docs.pytest.org/
- Selenium 文档: https://www.selenium.dev/documentation/
- Allure 文档: https://docs.qameta.io/allure/
- Jinja2 文档: https://jinja.palletsprojects.com/

---

> 💾 **保存提示**：将此文件收藏或打印，方便日常查阅！  
> 🔄 **持续更新**：随着学习深入，添加自己的笔记到这里
