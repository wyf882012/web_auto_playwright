# 🚀 Web自动化测试 - 7天快速上手指南

> 🎯 **目标**：7天内从零开始掌握Web自动化测试  
> ⏰ **每日学习**：2小时  
> 📋 **前提条件**：了解基础Python语法

---

## 📅 Day 1：环境搭建与第一次运行

### ✅ 今日目标
- [ ] 安装 Python 3.8+
- [ ] 配置项目依赖
- [ ] 安装浏览器驱动
- [ ] 成功运行示例用例

### 📝 步骤详解

#### 1. 检查 Python 版本
打开 PowerShell，运行：
```powershell
python --version
```
如果版本低于 3.8，请安装新版本：https://www.python.org/downloads/

#### 2. 安装项目依赖
```powershell
# 进入项目目录
cd e:\CODE\web_auto

# 安装依赖
pip install -r requeirement.txt

# 或者使用 requirements.txt（如果已修正文件名）
pip install -r requirements.txt
```

#### 3. 安装 Allure 工具

**方法一：使用 Scoop（推荐）**
```powershell
scoop install allure
```

**方法二：手动安装**
1. 下载：https://repo.maven.apache.org/maven2/io/qameta/allure/allure-commandline/
2. 解压到 `C:\allure`
3. 添加环境变量：`C:\allure\bin`

**验证安装**：
```powershell
allure --version
```

#### 4. 配置 ChromeDriver

**查看 Chrome 版本**：
1. 打开 Chrome 浏览器
2. 访问 `chrome://version/`
3. 记录版本号（如 `120.0.6099.130`）

**下载 ChromeDriver**：
1. 访问：https://googlechromelabs.github.io/chrome-for-testing/
2. 下载对应版本的 ChromeDriver
3. 解压到项目目录，如 `e:\CODE\web_auto\drivers\chromedriver.exe`

#### 5. 运行第一个测试

```powershell
python main.py --type=yaml --cases=./examples/web-cases-yaml
```

**预期输出**：
```
###############################################
######## 华测教育科技（http://vip.hctestedu.com）   
######## 自研多端融合自动化测试工具(版本v202507.1)      
######## 四合一：WEB/接口/APP/AI自动化      
######## 提升软件测试能力！认准(华测)全栈测试专家  
################################################
########开始环境检查#######
1. 检查allure-pytest是否存在
allure-pytest加载成功...
2. 检查当前环境中是否存在 allure 工具
allure 检查通过
...（浏览器启动，自动执行测试）
测试结束，开始生成测试报告...
```

测试完成后，会自动打开 Allure 测试报告页面 🎉

### 💡 今日学习重点

**观察以下文件**：
1. `main.py` - 程序入口
2. `examples/web-cases-yaml/context.yaml` - 配置文件
3. `examples/web-cases-yaml/0_DSW-登陆.yaml` - 第一个用例

**理解这些概念**：
- 命令行参数（`--type`, `--cases`）
- YAML 文件结构
- 测试报告的生成过程

---

## 📅 Day 2：理解 YAML 用例

### ✅ 今日目标
- [ ] 读懂 `context.yaml` 配置
- [ ] 理解用例文件结构
- [ ] 修改现有用例并运行
- [ ] 创建自己的第一个用例

### 📝 步骤详解

#### 1. 阅读配置文件

打开 `examples/web-cases-yaml/context.yaml`，重点关注：

```yaml
# 浏览器配置
_浏览器:
  capability:
    browserName: chrome

# 页面元素定位器
_WEB页面元素:
  手机号_输入框:
    定位方式: id
    目标对象: txtUName
  密码_输入框:
    定位方式: id
    目标对象: txtPassword
  登陆_按钮:
    定位方式: id
    目标对象: btnLogin
```

**理解**：
- `_浏览器`：告诉框架使用 Chrome 浏览器
- `_WEB页面元素`：给元素起中文名，用例中直接使用

#### 2. 阅读用例文件

打开 `examples/web-cases-yaml/0_DSW-登陆.yaml`：

```yaml
基础配置:
  用例类型: WebCase        # 区分测试类型
  一级模块: 我的登陆        # 报告中的一级分类
  二级模块: 登陆功能        # 报告中的二级分类
  用例标题: 登陆测试用例    # 具体用例名称

用例步骤:
- 进入到登录页面:           # 步骤名称（报告中显示）
    操作类型: 访问网址      # 调用哪个关键字
    网址: http://...        # 关键字的参数
- 输入用户名:
    操作类型: 输入内容
    数据内容: '{{username}}'  # 模板变量，从配置中获取
    _页面元素: 手机号_输入框  # 使用 context.yaml 中定义的元素
```

#### 3. 修改用例数据

在 `context.yaml` 中修改用户名和密码：

```yaml
username: '你的测试账号'
password: '你的测试密码'
```

重新运行测试，观察变化。

#### 4. 创建你的第一个用例

创建文件 `examples/web-cases-yaml/99_我的第一个用例.yaml`：

```yaml
基础配置:
  用例类型: WebCase
  一级模块: 学习测试
  二级模块: 基础操作
  用例标题: 访问读书屋网站

用例步骤:
- 打开读书屋首页:
    操作类型: 访问网址
    网址: http://novel.hctestedu.com
- 窗口最大化:
    操作类型: 窗口最大化
- 强制等待3秒:
    操作类型: 强制等待
    数据内容: '3'
```

运行测试：
```powershell
python main.py --type=yaml --cases=./examples/web-cases-yaml
```

**检查点**：
- [ ] 浏览器是否正常打开？
- [ ] 是否访问了指定网址？
- [ ] 报告是否正确生成？

### 💡 今日学习重点

**YAML 用例的 3 个核心部分**：
1. `基础配置` - 用例的元数据
2. `用例步骤` - 具体操作步骤
3. `数据驱动` - 多组测试数据（可选）

**常用关键字**：
- `访问网址` - 打开网页
- `输入内容` - 输入文本
- `点击元素` - 点击按钮
- `强制等待` - 暂停执行
- `窗口最大化` - 最大化浏览器

---

## 📅 Day 3：掌握关键字库

### ✅ 今日目标
- [ ] 阅读 `web_keywords.py` 代码
- [ ] 理解关键字的实现原理
- [ ] 添加一个自定义关键字
- [ ] 在用例中使用自定义关键字

### 📝 步骤详解

#### 1. 阅读关键字代码

打开 `HAT/keywords/web_keywords.py`，重点关注：

```python
class Keywords:
    def __init__(self, driver):
        self.driver = driver  # Selenium WebDriver 实例
    
    @allure.step("访问网址")
    def 访问网址(self, **kwargs):
        self.driver.get(kwargs['网址'])  # 核心就是一行代码
    
    @allure.step("输入内容")
    def 输入内容(self, **kwargs):
        eles_list = self.find_element(**kwargs)
        eles_list.send_keys(kwargs['数据内容'])  # 找到元素并输入
    
    @allure.step("点击元素")
    def 点击元素(self, **kwargs):
        eles_list = self.find_element(**kwargs)
        eles_list.click()  # 找到元素并点击
```

**理解**：
- 每个关键字就是一个 Python 方法
- `@allure.step` 装饰器让步骤显示在报告中
- `**kwargs` 接收 YAML 中的所有参数

#### 2. 理解元素定位

重点阅读 `find_element` 方法：

```python
def find_element(self, **kwargs):
    # 1. 从全局上下文获取所有元素定义
    all_ele_data = g_context().get_dict('_WEB页面元素')
    
    # 2. 根据用例中的 _页面元素 找到具体定位
    key = str(kwargs['_页面元素'])  # 如 "手机号_输入框"
    ele_data = all_ele_data[key]    # {'定位方式': 'id', '目标对象': 'txtUName'}
    
    # 3. 转换定位方式
    locator_type_map = {
        'id': By.ID,
        'xpath': By.XPATH,
        # ...
    }
    locator = (locator_type_map[ele_data['定位方式']], ele_data['目标对象'])
    
    # 4. 使用显式等待查找元素
    elements = self.wait_for_element(locator)
    return elements[0]
```

**执行流程**：
```
YAML: _页面元素: 手机号_输入框
  ↓
context.yaml: 手机号_输入框 -> {'定位方式': 'id', '目标对象': 'txtUName'}
  ↓
Selenium: driver.find_element(By.ID, 'txtUName')
```

#### 3. 添加自定义关键字

在 `HAT/keywords/web_keywords.py` 末尾添加：

```python
@allure.step("刷新页面")
def 刷新页面(self, **kwargs):
    """刷新当前页面"""
    self.driver.refresh()
    self.get_screenshot()
    logger.info("页面已刷新")

@allure.step("获取页面标题")
def 获取页面标题(self, **kwargs):
    """获取当前页面标题并保存到变量"""
    title = self.driver.title
    var_name = kwargs.get('变量名', 'page_title')
    g_context().set_dict(var_name, title)
    logger.info(f"页面标题: {title}")
    allure.attach(title, name="页面标题", attachment_type=allure.attachment_type.TEXT)
```

#### 4. 使用自定义关键字

修改 `99_我的第一个用例.yaml`：

```yaml
基础配置:
  用例类型: WebCase
  一级模块: 学习测试
  二级模块: 基础操作
  用例标题: 测试自定义关键字

用例步骤:
- 打开读书屋首页:
    操作类型: 访问网址
    网址: http://novel.hctestedu.com
- 获取页面标题:
    操作类型: 获取页面标题
    变量名: my_title
- 打印标题:
    操作类型: 强制等待
    数据内容: '2'
- 刷新页面:
    操作类型: 刷新页面
- 再次获取标题:
    操作类型: 获取页面标题
    变量名: my_title_after_refresh
```

运行测试，查看报告中是否显示了新的步骤。

### 💡 今日学习重点

**关键字的本质**：
- 就是 Python 方法
- 通过 `@allure.step` 装饰器集成到报告中
- 通过 `**kwargs` 接收 YAML 参数

**如何扩展关键字**：
1. 在 `web_keywords.py` 中添加方法
2. 方法名就是 YAML 中的 `操作类型`
3. 使用 `@allure.step` 添加报告步骤
4. 通过 `g_context()` 存取全局变量

---

## 📅 Day 4：数据驱动（DDT）

### ✅ 今日目标
- [ ] 理解数据驱动的概念
- [ ] 编写带数据驱动的测试用例
- [ ] 从数据库读取测试数据

### 📝 步骤详解

#### 1. 什么是数据驱动？

**场景**：测试登录功能，需要测试多种情况
- 正确的用户名和密码
- 错误的用户名
- 错误的密码
- 空用户名

**传统方式**：写 4 条几乎相同的用例
**数据驱动**：写 1 条用例模板 + 4 组数据

#### 2. 编写数据驱动用例

创建文件 `examples/web-cases-yaml/3_登录数据驱动.yaml`：

```yaml
基础配置:
  用例类型: WebCase
  一级模块: 用户模块
  二级模块: 登录功能
  用例标题: 登录测试

用例步骤:
- 访问登录页面:
    操作类型: 访问网址
    网址: http://novel.hctestedu.com/user/login.html
- 输入用户名:
    操作类型: 输入内容
    _页面元素: 手机号_输入框
    数据内容: '{{username}}'
- 输入密码:
    操作类型: 输入内容
    _页面元素: 密码_输入框
    数据内容: '{{password}}'
- 点击登录:
    操作类型: 点击元素
    _页面元素: 登陆_按钮
- 强制等待:
    操作类型: 强制等待
    数据内容: '2'

# 数据驱动：这里定义多组数据
数据驱动:
  - username: '13800138001'
    password: '123456'
    描述标题: 正确的用户名和密码
  - username: '错误用户名'
    password: '123456'
    描述标题: 错误的用户名
  - username: '13800138001'
    password: '错误密码'
    描述标题: 错误的密码
```

**执行结果**：
框架会自动生成 3 条测试用例：
1. 登录测试-正确的用户名和密码
2. 登录测试-错误的用户名
3. 登录测试-错误的密码

#### 3. 数据驱动的原理

查看 `HAT/parse/YamlCaseParser.py` 中的 `yaml_case_parser` 函数：

```python
def yaml_case_parser(config_path):
    case_infos = []  # 存储所有用例
    case_names = []  # 存储用例名称
    
    # 读取所有 YAML 文件
    yaml_case_infos = load_yaml_files(config_path)
    
    for case_info in yaml_case_infos:
        # 获取数据驱动数据
        ddts = case_info.get("数据驱动", [])
        
        if len(ddts) == 0:
            # 没有 DDT，直接添加
            case_name = case_info.get("基础配置").get("用例标题")
            case_names.append(case_name)
            case_infos.append(case_info)
        else:
            # 有 DDT，展开为多条用例
            for ddt in ddts:
                new_case = copy.deepcopy(case_info)  # 复制用例模板
                new_case.update({"local_context": ddt})  # 添加 DDT 数据
                
                # 生成用例名称：登录测试-正确的用户名和密码
                case_name = f'{原用例标题}-{ddt.get("描述标题")}'
                new_case.get("基础配置").update({"用例标题": case_name})
                
                case_names.append(case_name)
                case_infos.append(new_case)
    
    return {"case_infos": case_infos, "case_names": case_names}
```

#### 4. 前置脚本和后置脚本

**前置脚本**：在用例执行前运行的 Python 代码

```yaml
基础配置:
  用例标题: 使用前置脚本的用例

# 前置脚本：在用例执行前运行
前置脚本:
  - "context.update({'custom_var': '自定义值'})"
  - "import datetime; context.update({'current_time': str(datetime.datetime.now())})"

用例步骤:
- 访问页面:
    操作类型: 访问网址
    网址: http://example.com
- 输出自定义变量:
    操作类型: 强制等待
    数据内容: '{{custom_var}}'  # 使用前置脚本设置的变量

# 后置脚本：在用例执行后运行
后置脚本:
  - "print('用例执行完成')"
```

### 💡 今日学习重点

**数据驱动的优势**：
- 减少重复代码
- 易于维护
- 支持多种测试场景

**数据驱动的执行流程**：
```
YAML 用例模板
  ↓
读取 数据驱动 数据
  ↓
展开为 N 条用例
  ↓
每条用例独立执行
```

---

## 📅 Day 5：理解 pytest 插件

### ✅ 今日目标
- [ ] 理解 pytest 的基本概念
- [ ] 读懂 `CasesPlugin.py`
- [ ] 添加自定义 pytest 钩子

### 📝 步骤详解

#### 1. pytest 是什么？

pytest 是 Python 最流行的测试框架。

**简单示例**：
```python
# test_example.py
def test_addition():
    assert 1 + 1 == 2

def test_subtraction():
    assert 5 - 3 == 2
```

运行：
```bash
pytest test_example.py
```

#### 2. 本框架如何使用 pytest？

查看 `main.py`：

```python
# 1. 构建 pytest 参数
pytest_args = ["-v", "--no-header", "-s", "--clean-alluredir"]
pytest_args.append(f"--type={cmd_args.type}")
pytest_args.append(f"--cases={cmd_args.cases}")
pytest_args.append(TestRunner.__file__)  # 指定测试文件

# 2. 执行 pytest，传入自定义插件
exit_code = pytest.main(pytest_args, plugins=[CasesPlugin()])
```

**关键**：`plugins=[CasesPlugin()]` 传入了自定义插件

#### 3. 读懂 CasesPlugin

打开 `HAT/core/CasesPlugin.py`：

```python
class CasesPlugin:
    # 钩子1：添加命令行参数
    def pytest_addoption(self, parser):
        parser.addoption("--type", action="store", default="yaml", help="用例类型")
        parser.addoption("--cases", action="store", help="用例路径")
    
    # 钩子2：动态生成测试数据
    def pytest_generate_tests(self, metafunc):
        case_type = metafunc.config.getoption("type")
        cases_dir = metafunc.config.getoption("cases")
        
        # 调用解析器读取用例
        data = case_parser(case_type, cases_dir)
        
        # 参数化测试函数
        if "caseinfo" in metafunc.fixturenames:
            metafunc.parametrize(
                "caseinfo",           # 参数名
                data["case_infos"],   # 参数值
                ids=data["case_names"] # 用例名称
            )
    
    # 钩子3：修复中文乱码
    def pytest_collection_modifyitems(self, items):
        for item in items:
            item.name = item.name.encode("utf-8").decode("unicode_escape")
```

**理解**：
- `pytest_addoption`：让 pytest 认识 `--type` 和 `--cases` 参数
- `pytest_generate_tests`：动态生成测试数据（核心！）
- `pytest_collection_modifyitems`：修复中文显示问题

#### 4. 添加自定义钩子

在 `CasesPlugin.py` 中添加：

```python
class CasesPlugin:
    # ... 原有代码 ...
    
    # 新增：在所有测试结束后执行
    def pytest_terminal_summary(self, terminalreporter, exitstatus, config):
        print("\n" + "="*50)
        print("🎉 测试执行完成！")
        print(f"总计通过：{len(terminalreporter.stats.get('passed', []))}")
        print(f"总计失败：{len(terminalreporter.stats.get('failed', []))}")
        print("="*50)
```

### 💡 今日学习重点

**pytest 插件机制**：
- 通过钩子函数（hook）扩展 pytest 功能
- 钩子在特定时间点自动调用
- 本框架使用了 3 个钩子

**常用 pytest 钩子**：
- `pytest_addoption` - 添加命令行参数
- `pytest_generate_tests` - 动态生成测试数据
- `pytest_runtest_setup` - 每个测试前执行
- `pytest_runtest_teardown` - 每个测试后执行
- `pytest_terminal_summary` - 所有测试结束后

---

## 📅 Day 6：全局上下文与变量管理

### ✅ 今日目标
- [ ] 理解全局上下文的作用
- [ ] 掌握变量的传递方式
- [ ] 学习模板变量渲染

### 📝 步骤详解

#### 1. 什么是全局上下文？

全局上下文就像一个"共享数据仓库"，所有用例都可以读写。

**实现代码**（`HAT/core/globalContext.py`）：
```python
class g_context:
    _dic = {}  # 类属性，所有实例共享
    
    def set_dict(self, key, value):
        self._dic[key] = value
    
    def get_dict(self, key):
        return self._dic.get(key, None)
    
    def show_dict(self):
        return self._dic
```

**使用示例**：
```python
# 设置变量
g_context().set_dict('username', 'testuser')

# 获取变量
username = g_context().get_dict('username')

# 查看所有变量
all_vars = g_context().show_dict()
```

#### 2. 全局上下文的数据流动

**流程**：
```
1. 读取 context.yaml
   ↓
2. 存储到 g_context._dic
   ↓
3. YAML 用例中的 {{变量名}}
   ↓
4. VarRender.refresh() 渲染
   ↓
5. 替换为实际值
   ↓
6. 执行测试步骤
```

**实际示例**：

`context.yaml`:
```yaml
username: '13800138001'
password: '123456'
```

`用例.yaml`:
```yaml
用例步骤:
- 输入用户名:
    操作类型: 输入内容
    _页面元素: 手机号_输入框
    数据内容: '{{username}}'  # ← 模板变量
```

**执行时**：
```python
# 1. 获取全局上下文
context = g_context().show_dict()
# {'username': '13800138001', 'password': '123456', ...}

# 2. 渲染模板
from HAT.utils.VarRender import refresh
step_value = refresh("{{username}}", context)
# 结果: '13800138001'
```

#### 3. 跨用例传递数据

**场景**：登录用例获取 Token，后续用例使用 Token

**用例1：登录.yaml**
```yaml
用例步骤:
- 输入用户名:
    操作类型: 输入内容
    _页面元素: 用户名输入框
    数据内容: '{{username}}'
- 输入密码:
    操作类型: 输入内容
    _页面元素: 密码输入框
    数据内容: '{{password}}'
- 点击登录:
    操作类型: 点击元素
    _页面元素: 登录按钮
- 获取Token:
    操作类型: 获取元素文本
    _页面元素: Token元素
    变量名: user_token  # ← 保存到全局上下文
```

**用例2：查询.yaml**
```yaml
用例步骤:
- 使用Token查询:
    操作类型: 访问网址
    网址: http://example.com/api?token={{user_token}}
```

#### 4. 模板变量渲染详解

查看 `HAT/utils/VarRender.py`：

```python
from jinja2 import Template

def refresh(target, context):
    """
    渲染模板变量
    
    Args:
        target: 包含 {{变量名}} 的字符串
        context: 变量字典
        
    Returns:
        替换后的字符串
    """
    if target is None:
        return None
    return Template(str(target)).render(context)
```

**测试**：
```python
# 示例1：简单变量
result = refresh("Hello, {{name}}!", {"name": "World"})
# 结果: "Hello, World!"

# 示例2：多个变量
result = refresh("{{a}} + {{b}} = {{c}}", {"a": 1, "b": 2, "c": 3})
# 结果: "1 + 2 = 3"

# 示例3：None 处理
result = refresh(None, {"name": "World"})
# 结果: None
```

### 💡 今日学习重点

**全局上下文的 3 个核心方法**：
- `set_dict(key, value)` - 设置变量
- `get_dict(key)` - 获取变量
- `show_dict()` - 查看所有变量

**变量传递的 3 种方式**：
1. `context.yaml` 定义初始变量
2. 前置脚本动态设置变量
3. 测试步骤中提取变量（如 `获取元素文本`）

**模板渲染规则**：
- `{{变量名}}` 会被替换为实际值
- 支持复杂的 Jinja2 语法
- 找不到变量会保持原样或报错

---

## 📅 Day 7：实战项目与进阶

### ✅ 今日目标
- [ ] 为实际项目编写测试用例
- [ ] 使用断言验证测试结果
- [ ] 分析 Allure 测试报告
- [ ] 制定下一步学习计划

### 📝 步骤详解

#### 1. 实战：测试读书屋网站

**任务**：为 `http://novel.hctestedu.com` 编写完整的登录测试

**步骤1：创建配置文件**

创建 `my-cases/context.yaml`：
```yaml
session_reuse: False

_浏览器:
  capability:
    browserName: chrome

username: '15096268001'
password: '123456'

_WEB页面元素:
  用户名输入框:
    定位方式: id
    目标对象: txtUName
  密码输入框:
    定位方式: id
    目标对象: txtPassword
  登录按钮:
    定位方式: id
    目标对象: btnLogin
  我的书架链接:
    定位方式: xpath
    目标对象: //a[text()='我的书架']
```

**步骤2：编写测试用例**

创建 `my-cases/1_登录成功.yaml`：
```yaml
基础配置:
  用例类型: WebCase
  一级模块: 用户模块
  二级模块: 登录功能
  用例标题: 正常登录测试

用例步骤:
- 访问登录页面:
    操作类型: 访问网址
    网址: http://novel.hctestedu.com/user/login.html
- 输入用户名:
    操作类型: 输入内容
    _页面元素: 用户名输入框
    数据内容: '{{username}}'
- 输入密码:
    操作类型: 输入内容
    _页面元素: 密码输入框
    数据内容: '{{password}}'
- 点击登录:
    操作类型: 点击元素
    _页面元素: 登录按钮
- 强制等待:
    操作类型: 强制等待
    数据内容: '3'
- 断言登录成功:
    操作类型: 断言文本包含
    预期结果: 我的书架
    实际结果: 页面内容（需要先获取）
```

**步骤3：运行测试**
```powershell
python main.py --type=yaml --cases=./my-cases
```

#### 2. 使用断言验证结果

**常用断言**：
```yaml
# 断言文本包含
- 验证成功:
    操作类型: 断言文本包含
    预期结果: 欢迎
    实际结果: '{{welcome_text}}'

# 断言文本相等
- 验证标题:
    操作类型: 断言文本相等
    预期结果: 读书屋
    实际结果: '{{page_title}}'

# 断言数字相等
- 验证数量:
    操作类型: 断言数字相等
    预期结果: 5
    实际结果: '{{book_count}}'

# 断言数字大于
- 验证时间:
    操作类型: 断言数字大于
    预期结果: 0
    实际结果: '{{load_time}}'
```

#### 3. 分析 Allure 报告

打开生成的 HTML 报告，了解各个部分：

**报告结构**：
1. **概述** - 总览测试执行情况
2. **测试用例** - 详细的用例列表
3. **功能模块** - 按一级/二级模块分类
4. **时间轴** - 用例执行时间线
5. **图表** - 统计数据可视化

**每个用例详情**：
- 测试步骤
- 执行日志
- 截图
- 错误信息（如果失败）

#### 4. 制定学习计划

**掌握基础后，继续学习**：

□ **Week 2-3**：
- [ ] 学习 Excel 格式用例
- [ ] 学习数据库集成
- [ ] 学习扩展关键字

□ **Week 4-5**：
- [ ] 学习 pytest 高级用法
- [ ] 学习 Allure 报告定制
- [ ] 学习 Selenium 高级操作

□ **Week 6-8**：
- [ ] 学习 API 测试扩展
- [ ] 学习移动端测试
- [ ] 学习持续集成（CI/CD）

### 💡 今日学习重点

**实战要点**：
1. 先写配置文件
2. 再写测试用例
3. 运行测试
4. 查看报告
5. 调试问题
6. 优化用例

**断言的重要性**：
- 没有断言的测试是无效的
- 断言验证预期结果
- 断言失败说明有 bug

**持续学习**：
- 每天练习 1-2 小时
- 阅读框架源代码
- 尝试添加新功能
- 参与技术社区

---

## 🎓 学习检查清单

完成以下任务，证明你已经掌握了 Web 自动化测试：

### 基础任务（必须完成）
- [ ] 成功运行示例用例
- [ ] 编写 3 个自定义测试用例
- [ ] 添加 1 个自定义关键字
- [ ] 使用数据驱动编写用例
- [ ] 使用断言验证测试结果
- [ ] 看懂 Allure 测试报告

### 进阶任务（建议完成）
- [ ] 使用 Excel 格式编写用例
- [ ] 从数据库读取测试数据
- [ ] 创建自定义 pytest 钩子
- [ ] 添加失败重试机制
- [ ] 优化用例执行速度
- [ ] 集成到 CI/CD 流程

### 高级任务（挑战自己）
- [ ] 添加 API 测试支持
- [ ] 添加移动端测试支持
- [ ] 实现分布式并行执行
- [ ] 开发测试数据工厂
- [ ] 集成 AI 辅助测试
- [ ] 贡献代码到开源项目

---

## 📚 附录：常用命令速查

### 运行测试
```powershell
# 运行 YAML 用例
python main.py --type=yaml --cases=./examples/web-cases-yaml

# 运行 Excel 用例
python main.py --type=excel --cases=./examples/web-cases-excel

# 指定自定义关键字
python main.py --type=yaml --cases=./my-cases --keyDir=./my-keywords

# 指定报告路径
python main.py --type=yaml --cases=./my-cases --report_html_path=./my-report
```

### 安装依赖
```powershell
# 安装所有依赖
pip install -r requeirement.txt

# 安装并行执行插件
pip install pytest-xdist

# 安装失败重试插件
pip install pytest-rerunfailures
```

### 调试技巧
```powershell
# 查看详细输出
python main.py --type=yaml --cases=./my-cases -v -s

# 只运行某个用例（通过 pytest 参数）
pytest -k "登录" --type=yaml --cases=./my-cases

# 查看 pytest 帮助
pytest --help
```

---

> 🎉 **恭喜你完成 7 天学习之旅！**  
> 📖 继续阅读《学习指南_从零到精通.md》深入学习  
> 🔧 查看《项目检查报告.md》了解如何优化框架  
> 🚀 开始为你的实际项目编写自动化测试吧！
