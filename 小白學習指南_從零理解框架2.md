# HAT 自动化测试框架 - 3天零基础通关手册

> 🎯 **学习目标**：本指南专为编程小白设计。我们将通过“总-分”结构，把复杂的代码拆解为生活常识。只需3天，你就能从零开始，独立编写并运行自动化测试脚本。

---

## 📅 3天学习路线图

*   **第1天：看懂地图** —— 理解框架是干什么的，目录里每个文件扮演什么角色。
*   **第2天：拆解引擎** —— 深入代码内部，通过“为什么”问答，搞懂机器人是如何思考的。
*   **第3天：实战演练** —— 亲手写出第一个脚本，并学会如何修复报错。

---

## 第一部分：总览——自动化测试的“全自动餐厅”模型

在深入代码之前，我们先建立一个宏观认知。请把 `HAT` 框架想象成一家**全自动餐厅**：

| 餐厅组件 | 对应项目文件 | 作用说明 |
| :--- | :--- | :--- |
| **顾客点单** | `examples/*.yaml` | 你写的测试用例，告诉机器人要测什么（比如：登录、搜索）。 |
| **前台经理** | `main.py` | 接收你的指令，指挥后厨开始工作。 |
| **中央厨房** | `HAT/core/` | 核心大脑，负责调度资源、控制流程。 |
| **预制菜谱** | `HAT/keywords/` | 封装好的动作库（如：点击、输入），厨师直接拿来用。 |
| **食材仓库** | `context.yaml` | 存放账号、密码、元素定位等公共数据。 |
| **美食评价表** | `HTML测试报告/` | 自动生成的精美报告，展示测试结果。 |

---

## 第二部分：拆分——执行流程的深度拆解

### 环节一：准备食材（数据准备）
*   **这是什么**：在 `context.yaml` 中定义全局变量。就像做菜前先把盐、糖准备好。
*   **核心代码**：`HAT/parse/YamlCaseParser.py` 中的 `load_context_from_yaml()`。
*   **小白疑问**：为什么要单独放一个文件？
    *   **解答**：为了“一处修改，处处生效”。如果网站地址变了，你只需要改 `context.yaml`，不用去改几十个测试脚本。

### 环节二：翻译菜单（用例解析）
*   **这是什么**：把你写的 YAML 语言翻译成 Python 字典。
*   **核心代码**：`HAT/parse/YamlCaseParser.py` 中的 `yaml_case_parser()`。
*   **小白疑问**：为什么不能直接运行 YAML？
    *   **解答**：Python 听不懂 YAML。解析器就像一个翻译官，把“访问网址: www.baidu.com”翻译成 Python 能执行的 `{'操作类型': '访问网址', '网址': 'www.baidu.com'}`。

### 环节三：大厨炒菜（执行引擎）
*   **这是什么**：`TestRunner.py` 拿着翻译好的字典，一步步调用关键字。
*   **核心代码**：`HAT/core/TestRunner.py` 第 56-85 行的循环逻辑。
*   **小白疑问**：它是怎么知道该调用哪个函数的？
    *   **解答**：通过 `key_func = keywords.__getattribute__(key)`。这行代码会根据字典里的“操作类型”名字，去关键字库里找同名的函数来执行。

### 环节四：成品装盘（报告生成）
*   **这是什么**：收集每一步的截图和日志，生成 HTML 报告。
*   **核心代码**：`main.py` 第 83 行的 `allure generate` 命令。

---

## 第三部分：深入——核心代码的“十万个为什么”

### Q1: 为什么关键字函数都要写 `**kwargs`？
*   **场景**：你在 `web_keywords.py` 看到 `def 输入内容(self, **kwargs):`。
*   **通俗解释**：因为不同的菜需要的调料不一样。有的步骤需要“网址”，有的需要“等待时间”。`**kwargs` 就像一个万能口袋，不管传进来多少个参数，它都能接住，不会报错。

### Q2: 为什么要有 `g_context` (全局上下文)？
*   **场景**：代码里到处都在用 `g_context().get_dict()`。
*   **通俗解释**：它就像餐厅的“公共黑板”。厨师 A 在黑板上写下“今天特价菜是鱼”，厨师 B 抬头就能看到。如果没有这个黑板，每个厨师都得互相打电话问数据，效率太低且容易乱。

### Q3: 为什么要用 `WebDriverWait` (显示等待)？
*   **场景**：`find_element` 函数里包裹了一层等待逻辑。
*   **通俗解释**：网页加载像外卖配送，有时快有时慢。如果你刚下单（打开网页）就急着去门口拿（找元素），可能外卖员还没到。显示等待就是让你最多等 10 秒，外卖一到立马开吃，既不误事也不傻等。

---

## 第四部分：实战——3天通关任务清单

### ✅ 第1天任务：环境搭建与观察
1.  **安装依赖**：运行 `pip install -r requeirement.txt`。
2.  **运行示例**：在终端输入 `.
un.ps1 --type=yaml --cases=./examples/web-cases-yaml`。
3.  **观察报告**：打开弹出的 `HTML测试报告`，看看绿色的通过标记长什么样。

### ✅ 第2天任务：模仿与修改
1.  **打开模板**：找到 `examples/web-cases-yaml/0_DSW-登陆.yaml`。
2.  **修改数据**：尝试修改其中的 `username` 或 `password`，再次运行，观察报告中的变化。
3.  **阅读源码**：打开 `HAT/keywords/web_keywords.py`，找到 `def 点击元素`，看看它是怎么实现点击的。

### ✅ 第3天任务：独立创作
**挑战目标**：创建 `6_DSW-退出登录.yaml`。

```yaml
基础配置:
  用例类型: WebCase
  一级模块: 用户中心
  用例标题: 退出登录验证

用例步骤:
- 访问首页:
    操作类型: 访问网址
    网址: http://novel.hctestedu.com/
- 点击退出:
    操作类型: 点击元素
    _页面元素: 退出按钮
- 验证结果:
    操作类型: 断言浏览器路径
    数据内容: 'http://novel.hctestedu.com/user/login.html'
```

**成功标准**：运行后，报告中出现“退出登录验证”且状态为绿色。

---

## 💡 遇到问题怎么办？（排错三部曲）

1.  **第一步：看日志**。去 `logs/` 文件夹找最新的 `.log` 文件，搜索 `ERROR`。
2.  **第二步：看截图**。在 Allure 报告里点进失败的用例，看最后一步截图停在哪。
3.  **第三步：查缩进**。90% 的 YAML 报错都是因为用了 Tab 键或者缩进没对齐，记得全部换成空格。

---

**结语**：
自动化测试并不难，它只是把人的手工操作变成了机器能读懂的“说明书”。按照这份指南走一遍，你就已经跨过了最难的第一道门槛！加油！🚀
